const AuthKeys = {
  NAME: "auth",
  ACCESS_TOKEN: "accessToken",
  REFRESH_TOKEN: "refreshToken",
  USER_ID: "userId",
};

const UserKeys = {
  NAME: "user",
};

const RouteKeys = {
  LOGIN: "login",
  SIGNUP: "signup",
  LOGOUT: "logout",
  ADMIN: "admin",
  CALLBACK: "callback",
};

export default {
  AuthKeys,
  UserKeys,
  RouteKeys,
};
